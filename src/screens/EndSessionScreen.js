import React, { PureComponent } from 'react';
import { View, Text } from 'react-native';

class EndSessionScreen extends PureComponent {
    constructor(props) {
        super(props);
        this.state = {
        };
    }

    render() {
        return (
            <View>
                <Text> EndSession </Text>
            </View>
        );
    }
}

export default EndSessionScreen;